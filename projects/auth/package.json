{
  "name": "@frontegg/ng-auth",
  "version": "1.0.1",
  "lib": "./dist",
  "scripts": {
    "build:watch": "ng build --watch --project=auth",
    "build": "ng build --prod --project=auth",
    "test": "ng test --project=auth",
    "lint": "ng lint --project=auth"
  },
  "peerDependencies": {
    "@angular/common": "^10.1.3",
    "@angular/core": "^10.1.3",
    "@frontegg/ng-core": "^0.0.1"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  }
}
