{
  "name": "@frontegg/ng-connectivity",
  "version": "1.0.0",
  "lib": "./dist",
  "scripts": {
    "build:watch": "ng build --watch --project=connectivity",
    "build": "ng build --prod --project=connectivity",
    "test": "ng test --project=connectivity",
    "lint": "ng lint --project=connectivity"
  },
  "peerDependencies": {
    "@angular/common": "^10.1.3",
    "@angular/core": "^10.1.3",
    "@frontegg/ng-core": "^0.0.1"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  }
}
